<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Order Confirmation</title>
</head>
<body>
    <h1>Order Confirmation</h1>
    
    <h2>Thank you for your order, <%= order.user.username %>!</h2>
    <p>Your order has been placed successfully.</p>
    <p><strong>Order Date:</strong> <%= order.date.toLocaleString() %></p>
    <h3>Order Details:</h3>
    <ul>
        <% order.items.forEach(item => { %>
            <li>
                <img src="<%= item.image %>" alt="<%= item.name %>" style="width:100px;height:100px;">
                <strong><%= item.name %></strong> - $<%= item.price %> x <%= item.quantity || 1 %>
            </li>
        <% }) %>
    </ul>
    <p><strong>Total Price: $<%= order.totalPrice %></strong></p>

    <a href="/">Continue Shopping</a>
    <a href="/profile">View Profile</a>
    <a href="/logout">Logout</a>
</body>
</html>